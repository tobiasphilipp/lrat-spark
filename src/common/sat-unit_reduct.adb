package body SAT.Unit_Reduct 
with SPARK_Mode => On
is

   procedure Propagate_Lemma
     (A  : in Assignment_Type;
      Ap : in Assignment_Type;
      F  : in Formula_Type)
   is
   begin
      null;
   end Propagate_Lemma;
   
   procedure Propagate_RUP_Lemma (A  : in Assignment_Type;
      Ap : in Assignment_Type;
      C  : in Clause_Type;
                            F  : in Formula_Type)
   is
   begin
      null;
   end Propagate_RUP_Lemma;

end SAT.Unit_Reduct;
