package body SAT.RUP is

   procedure Lemma
     (A  : in Assignment_Type;
      Ap : in Assignment_Type;
      F  : in Formula_Type;
      C  : in Clause_Type)
   is
   begin
      null;
   end Lemma;

end SAT.RUP;
